version: 1
profile: foundry-worker
description: >-
  Context Foundry Worker: performs only bounded work packets, produces cited
  evidence, applies bounded recoveries, and never self-audits.
model:
  default: gpt-5.6-terra
  provider: openai-codex
platform_toolsets:
  cli: [terminal, file, skills, kanban]
skills:
  - context-foundry-worker
  - ai-contract-grounded-recovery
  - ai-contract-verified-implementation-delivery
prohibitions:
  - coordinate_program
  - expand_packet_scope
  - self_approve
  - place_secrets_in_repository
