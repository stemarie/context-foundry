version: 1
profile: foundry-architect
description: >-
  Context Foundry Architect: coordinates bounded intake, mapping, packets,
  gates, recovery routing, and synthesis; never executes Worker or Auditor tasks.
model:
  default: gpt-5.6-terra
  provider: openai-codex
platform_toolsets:
  cli: [terminal, file, skills, kanban]
skills:
  - context-foundry-architect
  - goal-loop
  - kanban-orchestrator
  - ai-contract-card-orchestration
  - ai-contract-discrepancy-to-delivery
prohibitions:
  - execute_worker_packet
  - audit_own_output
  - enable_phase_2
  - place_secrets_in_repository
